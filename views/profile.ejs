
<p></p>
<p class="lead mb-3">
    
    <div class="card">
        <% if (user.userimage) { %>
            <img src="/static/uploads/<%=user.userimage %>" alt="image" style="width:100%">
        <%} else { %>
            <img src="/static/images/no_person.jpg" alt="<%=user.first_name %>" style="width:100%">
        <% } %>
        
        <h2><%=user.first_name %> <%=user.last_name %></h2>
        <p class="title"><%=user.email %></p>
        <p>Harvard University</p>
    </div>
</p>
<p class="lead" style="text-align:center">
    <a class="btn btn-primary btn-lg" href="/edit_profile" role="button">Edit Profile</a>
    <a class="btn btn-primary btn-lg" href="/change_password" role="button">Change Password</a>
</p>

<style>
    .card {
      box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
      max-width: 300px;
      margin: auto;
      text-align: center;
    }

    .title {
      color: grey;
      font-size: 18px;
    }

    button {
      border: none;
      outline: 0;
      display: inline-block;
      padding: 8px;
      color: white;
      background-color: #000;
      text-align: center;
      cursor: pointer;
      width: 100%;
      font-size: 18px;
    }

    a {
      text-decoration: none;
      font-size: 22px;
      color: black;
    }

    button:hover, a:hover {
      opacity: 0.7;
    }
</style>